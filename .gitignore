# User-specific stuff
.idea
.DS_Store
.claude

# Runtime data (created by install.sh on the server)
data/excluded.txt
data/tld_exceptions.txt
data/sync/
data/credentials/

# Local build output — build.sh writes to build/ so it never touches the
# tracked tarball at the repo root (that one is CI-managed only).
build/
