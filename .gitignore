# User-specific stuff
.idea
.DS_Store
.claude

# Runtime data (created by install.sh on the server)
data/excluded.txt
data/tld_exceptions.txt
data/sync/
data/credentials/

# Build artifact — produced by build.sh locally for testing, but the
# canonical copy on main is written by the release workflow on each tag.
# Don't commit local rebuilds.
da_dnssec_sync_manager.tar.gz
